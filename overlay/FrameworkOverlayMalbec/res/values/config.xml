<?xml version="1.0" encoding="utf-8"?>
<!--
     SPDX-FileCopyrightText: The LineageOS Project
     SPDX-License-Identifier: Apache-2.0

     Every value here was read off the running stock ROM, from
     work/device_dump/display/dumpsys-display.txt, rather than copied from
     another device. The relevant line is the DisplayDeviceInfo for
     "Built-in Screen".

     Nothing panel specific belongs in this file. The panel is dual sourced
     (BOE nt36536e / CSOT nt36536) and is selected by the bootloader on the
     kernel command line, resolved inside the prebuilt dtbo. Both suppliers run
     the same mode list, so these values hold for either one.
-->
<resources>
    <!-- The panel runs 30/60/90/120/144.

         ⚠️ config_defaultRefreshRate was 60 here, on the reasoning that 60 is
         "the rate the display settles to when nothing is driving it". That
         reasoning was inferred from an idle renderFrameRate reading rather than
         read off the resource, and it is wrong. DisplayModeDirector on the
         running stock ROM reports

             mDefaultRefreshRate: 144.0
             mDefaultPeakRefreshRate: 240.0

         mDefaultRefreshRate is R.integer.config_defaultRefreshRate, so stock
         sets it to 144 (via FrameworksResTarget_Vendor.apk, the vendor RRO this
         port replaces). At 60 it posts a PRIORITY_DEFAULT_RENDER_FRAME_RATE
         vote (DisplayModeDirector.java:1217-1220) that caps the render frame
         rate globally at 60 — on a 144 Hz panel.

         ★ Both are 120, not 144, and that is deliberate: THE ACTIVE STYLUS DOES
         NOT WORK AT 144 Hz. This is measured on this unit, both directions, with
         the pen in the owner's hand and /proc/support_pen already set to 1:

             peak_refresh_rate = 120.0   ->  6746 pen events, 438 ABS_PRESSURE
                                             samples over 23 strokes, pressure a
                                             smooth ramp 0x365 -> 0x8a2 -> back
                                             down, and /proc/Pen_ID resolving to
                                             "2;87;..." (pen type 2, battery 87%)
             peak_refresh_rate = 144.0   ->  0 pen events. Not "no pressure" -
                                             zero. /proc/Pen_ID falls straight
                                             back to 255;255;0xffff;0xffff.

         So at 144 Hz the controller cannot even detect the pen, let alone decode
         its uplink. That also rules out any auto-switching scheme that watches
         the digitizer for a pen: at 144 Hz the digitizer is blind, so there is
         nothing to watch. Chicken and egg.

         Stock hits the same wall and solves it in the framework. ZUI patches
         DisplayModeDirector with judgePenSupport()/supportPen(boolean):

             supportPen(true):   updateGlobalVote(26, forRenderFrameRates(0, 120f))
                                 setHighReportRate(0)
             supportPen(false):  updateGlobalVote(26, null)
                                 setHighReportRate(1)

         i.e. it votes the render rate down to exactly 120 whenever the pen is in
         use, and swaps the touch controller between two mutually exclusive
         modes - high finger report rate, or active-pen scanning, never both.
         judgePenSupport() picks between them from Settings.System
         auto_refresh_support_pen_switch (a user toggle) and is_pen_attached,
         the latter fed by BluetoothPenConnectPolicy over the pen's BLE link.

         We have neither that framework patch nor the BLE plumbing, so the choice
         is between "144 Hz and no stylus" and "120 Hz and a working stylus" -
         and this device ships with a stylus. 120 vs 144 is a 1.4 ms difference
         in frame time; a stylus that produces no events at all is not a
         trade-off. Both states this leaves reachable work with the pen: Smooth
         Display on gives 120 (config_defaultPeakRefreshRate), off gives 60.

         ★ Consequence, and it is the opposite of what this file used to say.

         The old text here claimed /proc/HighReportRate "stays at the driver
         default 0, and that is now CORRECT rather than an omission - it is
         exactly the value stock uses in its supportPen(true) state". True as far
         as it went, but it left the tablet permanently in stock's PEN state and
         quietly gave up the fastest touch this hardware has. Measured, 8 s per
         window, panel rate read back inside every window
         (work/scripts/61-touch-rate-at-144.sh; raw evdev in work/s18/touch-rate/):

             panel  HighReportRate   finger      pen
             120    0                120 Hz      240 Hz
             120    1                360 Hz      none
             144    0                185 Hz      none
             144    1                185 Hz      none

         The top finger report rate is ~360 Hz and it exists ONLY at 120 Hz. Above
         120 the controller caps at ~185 Hz and HighReportRate stops having any
         effect at all - so 144 Hz costs the stylus AND half the touch sampling,
         and stock's own no-pen state (144 + HRR 1) never reaches 360 Hz either.
         120 + HRR 1 is a corner ZUI does not enter.

         That is why this tree ships TWO modes at the same 120 Hz, differing in
         the touch controller and the thermal policy rather than in the panel rate
         (MalbecParts, "Device mode"), and why the refresh-rate picker is removed
         from Settings altogether (overlay/SettingsOverlayMalbec). There is no
         configuration above 120 Hz in which a user of this device is better off.

         The old instruction here - "to trade the stylus back for 144 Hz,
         settings put system peak_refresh_rate 144.0" - still physically works,
         but it now also costs half the touch sampling, and MalbecParts will
         notice, say so in a toast, and put it back. It is not a supported state.

         ★ DO NOT "improve" this into stock's dynamic scheme. That was the
         obvious next step and the device owner rejected it, with the better
         argument: switching costs more than the 24 Hz buys. Every transition is a
         DSI mode change on the panel PLUS a mode change in the touch controller,
         both visible and both costing power, and they would fire every time the
         pen enters and leaves range. Pinning the value also deletes a whole class
         of state-machine bugs (pen arriving mid-switch, switch racing the idle
         timer, the chicken-and-egg above). Static is the design, not a stopgap.

         Of the two, config_defaultPeakRefreshRate is the load-bearing one, and
         it is worth knowing why. It is the default of Settings.System
         PEAK_REFRESH_RATE, and updateRefreshRateSettingLocked()
         (DisplayModeDirector.java:1202-1209) turns that into
             Vote.forPhysicalRefreshRates(0, peak)  @ PRIORITY_USER_SETTING_PEAK_REFRESH_RATE
         - a ceiling on the PHYSICAL panel mode, which is what the pen actually
         needs.

         config_defaultRefreshRate is 0, and that is not "unset" - it is AOSP's
         documented way to say "the peak setting is the default"
         (core/res/res/values/config.xml:5860-5862: "If a non-zero value is set
         for config_defaultPeakRefreshRate, then config_defaultRefreshRate may be
         set to 0, in which case the value set for config_defaultPeakRefreshRate
         will act as the default frame rate.").

         ⚠️ It used to be 120 here, "for coherence", and that silently capped the
         game tier. updateRefreshRateSettingLocked (DisplayModeDirector.java:
         1217-1220) turns a non-zero value into
             Vote.forRenderFrameRates(0, default)
         registered with updateGlobalVote() - a GLOBAL render ceiling that no
         per-display peak vote can lift. Measured on the device, all three rows
         from one sitting:

             peak=120            physical (0,120)   render (0,120)   mode 3
             peak=144            physical (0,144)   render (0,120)   <- capped
             peak=144 + min=144  physical (144,144) render (144,144) mode 2, 144 Hz

         So with the old value, choosing game mode raised the panel's ceiling and
         then throttled the render rate back to 120 anyway: half a mode change,
         which is worse than either state. Setting it to 0 removes the global vote
         entirely; the ceiling in each mode then comes only from
         PEAK_REFRESH_RATE, which MalbecParts owns.

         The "both are 0" error branch at :1223-1229 cannot trigger: peak falls
         back to config_defaultPeakRefreshRate = 120 when the setting is unset.

         ★ SESSION 33 - THESE TWO VALUES ARE NO LONGER THE WHOLE STORY.

         Everything above is about the CEILING, and a ceiling is only half a
         policy. The floor was 0, so SurfaceFlinger was free to take the panel to
         the lowest mode it has whenever nothing was animating: measured 30 Hz,
         with a residency of 38m49s against 120 Hz's 20m11s in a single session.
         30 Hz visibly flickers on this LCD.

         The floor CANNOT be set from this file, and that is worth stating so
         nobody looks for the resource. Settings.System.MIN_REFRESH_RATE has a
         hardcoded 0f default (DisplayModeDirector.java:1169) - no config_*, no
         def_ in SettingsProvider - so the only lever is a write, and
         MalbecParts.PenModeController now pins min to the same ceiling as peak
         and sets Settings.Secure.match_content_frame_rate to
         MATCH_CONTENT_FRAMERATE_NEVER. The row above therefore reads differently
         now:

             peak=120 + min=120  physical (120,120) render (120,120) mode 3, pinned
                                 appRequest (120,120) once switching is NEVER

         ⚠️ Which makes config_defaultPeakRefreshRate MORE load-bearing than the
         text above implies, not less. Before the pin, a stray 144 was a rate the
         panel visited; now it is the rate the panel SITS at, and the digitizer
         cannot see the pen there at all. Do not raise this value.

         ⚠️ And do not read the floor out of `dumpsys display`. mActiveModeId
         there is the DESIRED mode and reported 120 for the entire time the panel
         was physically at 30. `dumpsys SurfaceFlinger` is the only source, and
         its per-mode residency counters are the instrument - a single sample
         cannot see a device that spends most of its awake life somewhere else.

         Note also that 144 Hz remains in Display > Refresh rate, because that
         list is built at runtime from Display.getSupportedModes()
         (RefreshRateSettingsUtils.java:43) and no RRO can filter it. Deliberate: the
         alternatives were patching the panel timings in dtbo.img (a partition
         this port never flashes, precisely because ours is byte-identical to
         stock) or carrying the tree's only out-of-device-tree source patch. Apps
         cannot reach 144 regardless - the app-request range only considers votes
         at priority >= APP_REQUEST_REFRESH_RATE_RANGE_PRIORITY_CUTOFF (5) and
         the peak setting sits at 9, so it bounds them. A human choosing 144 in
         Settings can do it, and MalbecParts turns that into a full, coherent
         switch to game mode rather than letting the pen fail silently: it watches
         PEAK_REFRESH_RATE, MIN_REFRESH_RATE and (since session 33)
         Settings.Secure.match_content_frame_rate, and toasts what happened. -->
    <!-- Default to LIGHT, not dark.

         AOSP's own default is already 1 / MODE_NIGHT_NO (config.xml:898). Dark
         mode on this device comes from PixelConfigOverlay2021_GMS, which sets
         this resource to 2. ⚠️ On LineageOS the competing value has a different
         source and PixelConfigOverlay2021_GMS does not exist: it is
         vendor/lineage/overlay/common/frameworks/base/core/res/res/values/
         config.xml:153, a STATIC overlay compiled into framework-res.apk.
         That matters because this RRO overrides the value compiled into the
         target package without changing Android's standard partition order.

         It is wrong here on the hardware: this panel is an LCD, not OLED (the
         backlight driver logs [AWNl/E]lcd_brightness_work), so dark mode buys no
         power and costs contrast, and the device owner reported being unable to
         read the UI.

         The installed product overlays currently do not define this resource,
         so no global partition-precedence override is needed. Verify with:
             adb shell cmd overlay lookup android android:integer/config_defaultNightMode
         which must return 1.

         This is only the DEFAULT; Settings > Display > Dark theme still wins,
         because UiModeManagerService:653 reads Settings.Secure.UI_NIGHT_MODE and
         falls back to this resource only when that is unset. -->
    <integer name="config_defaultNightMode">1</integer>

    <integer name="config_defaultPeakRefreshRate">120</integer>
    <integer name="config_defaultRefreshRate">0</integer>

    <!-- Rounded display corners. Stock gets this from the same vendor RRO as
         config_defaultRefreshRate above; ro.sf.rounded_corner_radius is unset on
         this device, so without an override here SystemUI would not mask the
         corners at all.

         Keeping the unit as px is right: the corner radius is a property of the
         glass, so it must not move when TARGET_SCREEN_DENSITY does.

         The value, though, has been wrong twice, in opposite directions, and
         both times because the dp -> px conversion was anchored to the wrong
         density. Writing the derivation down so it stops happening:

             stock RRO declares    20dp
             stock default density 360   ->  20 * 360/160 = 45 px
             dump-time density     306   ->  20 * 306/160 = 38.25 -> 38 px

         ⚠️ THIS TREE SHIPS DENSITY 320, not 360. BoardConfig.mk:366 is
         TARGET_SCREEN_DENSITY := 320 (owner's decision, frozen), and the running
         device confirms it: `wm density` reports 320 and dumpsys display prints
         density 320. So at the density we actually ship the privacy-dot floor is
         20 * 320/160 = 40 px, not 45.

         The shipped 45 px is still CORRECT, because the framework takes
         max(ongoing_appops_dot_min_padding, roundedCornerPadding) and 45 > 40 —
         the bar stays symmetric. But the derivation above no longer explains the
         number, and the next person to "recompute it properly" would arrive at 40
         and think they had found a bug. 45 px = 22.5 dp at 320, chosen because it
         is what stock renders and the owner's eye was calibrated on stock.

         An earlier revision said 45px and cited a dumpsys measurement; that was
         the right number with the wrong evidence. It was then "corrected" to
         38px because work/device_dump/display/dumpsys-display.txt:71 does report
             RoundedCorner{position=TopLeft, radius=38, center=Point(38, 38)}
         and wm.txt records "Override density: 306".

         But 306 was not a stock default and not a menu option misread as a
         value: the owner had set it by hand in Settings > Display size before
         that dump was taken (confirmed by the owner). It says nothing about the
         hardware. Stock's own default is 360: wm.txt:2 says "Physical density:
         360", and dumpsys-display.txt:201 mBaseDisplayInfo density 360 versus
         :202 mOverrideDisplayInfo density 306. So the radius Lenovo dimensioned
         the glass against is 45 px, and 38 was an artefact of one user setting.

         Confirmed directly rather than inferred: aapt2 dump of the stock vendor
         RRO reports
             work/unpacked/parts/vendor/overlay/FrameworksResTarget_Vendor.apk
             -> dimen/rounded_corner_radius  20dp
         and stock's own framework-res.apk leaves it at 0dp, so the RRO is where
         it comes from.

         ⚠️ This paragraph used to read "TARGET_SCREEN_DENSITY is now 360, the
         same as stock, so 45px and 20dp are the same number here" — written
         while the tree was briefly on 360 and never corrected when the owner
         set 320 and froze it (BoardConfig.mk:366, and the warning 34 lines
         above). At the shipped 320, stock's 20dp is 40px, not 45px.
         The shipped value is 33px and is the owner's own choice, arrived at by
         looking at the glass, so the arithmetic above is background rather than
         a derivation of it.

         Keep the unit as px regardless: the radius is a property of the glass
         and must not move if the density is ever retuned. That is the whole
         point of expressing it in px.

         ⚠️ Not 156px, which is what work/reference/hirero-malbec-device puts
         here. At 320 real ppi that would be 12.4 mm of corner masking on a
         panel whose vendor RRO says 3.6 mm.

         ★ 60px, not stock's 45px, and this one is an EXPERIMENT rather than a
         derivation - say so plainly so the next person does not read it as
         settled. At 45px the owner photographed a bright crescent of lit panel
         at all four corners (work/s14/corner-white.png, zoomed in
         work/s14/crescent-zoom.png, taken on a solid white screen; there is no
         screen protector on the device, that was checked).

         What the photo does establish: the crescent's INNER edge is visibly
         stair-stepped, i.e. rasterised, so it is a rendered boundary and not an
         optical artefact of the glass; its outer edge is smooth, i.e. physical.
         So SystemUI's mask really is stopping short of where the panel stops
         lighting.

         What it does NOT establish is the sign. Pure geometry says a mask arc
         and a glass arc should never produce a crescent BRIGHTER than the screen
         around it: with mask radius Rm and glass radius Rg, Rg>Rm hides the mask
         entirely (no artefact at all) and Rg<Rm gives a BLACK crescent. A
         brighter-than-screen arc fits neither, so something about the assumed
         geometry is wrong and guessing harder from one oblique photo was not
         going to fix it.

         The value was found by bisection on the device, because the arc model
         I reasoned from did not survive contact: it predicted a black crescent
         for Rm>Rg and no artefact at all for Rm<Rg, and the observed crescent
         was BRIGHTER than the surrounding screen, which is neither. Something
         in that model is wrong and was never found. So this is empirical.

             45px (stock's own 20dp @ 360) -> thin bright crescent, all 4 corners
             60px                          -> crescent GREW  => go down, not up
             36px                          -> "almost gone" (owner, on a white
                                              screen photographed with another
                                              camera)
             35px                          -> this value

         Facts worth not re-deriving:
           - the crescent's inner edge is stair-stepped, i.e. rasterised, so it
             really is SystemUI's mask and not an optical artefact of the bevel;
             its outer edge is smooth, i.e. physical
           - there is no screen protector on this device (asked, confirmed)
           - screencap CANNOT see any of this: AOSP deliberately excludes
             ScreenDecorOverlay from screenshots, so every check needs a photo
             of a solid white screen taken with a second camera. Do not try to
             verify this from a screencap and conclude the corners are square.

         ★ 33px is the device owner's own setting. They walked it down the
         bracket themselves, 35 -> 34 -> 33, living with each one; this is where
         they stopped. Keep it. Going back up above 35 has been established as
         pointless, and below 33 the crescent starts coming back. -->
    <dimen name="rounded_corner_radius">33px</dimen>

    <!-- ★ The status bar's left and right content insets come from HERE, not
         from any SystemUI dimension, and leaving it at AOSP's 0dp default is
         what the device owner saw as "no gap at the left/right edges".

         StatusBarContentInsetsProvider.getCalculatedAreaForRotation()
         (frameworks/base/packages/SystemUI/src/com/android/systemui/statusbar/
         layout/StatusBarContentInsetsProvider.kt:334-364) is the only place
         those insets are computed, and it computes them as

             roundedCornerPadding = rounded_corner_content_padding
             minLeft  = roundedCornerPadding
             minRight = max(ongoing_appops_dot_min_padding, roundedCornerPadding)

         Note what is NOT in that expression: status_bar_padding_start /
         status_bar_padding_end. Those two are commented out in the same file
         (:100-104, "TODO: Check if this is ever used for devices with no
         rounded corners") and only survive as view padding inside
         PhoneStatusBarView.updatePaddings(). So overriding them is not the knob.

         With rounded_corner_content_padding at 0 the two sides come out
         ASYMMETRIC, because only the right side has a floor:

             left  = 0px
             right = max(ongoing_appops_dot_min_padding = 20dp = 45px, 0) = 45px

         Measured on the device rather than derived: a screencap of the home
         screen puts the clock glyphs ~22px from the left edge (the 4dp
         status_bar_padding_start plus the font's left side bearing) and the
         battery icon ~55px from the right. That 20dp step is visible, and it is
         the whole of the complaint.

         45px restores symmetry: minLeft becomes 45px and minRight stays 45px,
         because any value at or above the privacy-dot floor makes the two sides
         equal.

         ⚠️ The arithmetic in the two paragraphs above is stale and is left
         corrected rather than deleted, because it is quoted elsewhere. It read
         "45px is exactly the dot floor (ongoing_appops_dot_min_padding = 20dp at
         density 360)". This tree ships density 320 (BoardConfig.mk:366, frozen by
         the owner), where 20dp is 40px, not 45px. So the floor is 40px and the
         shipped 45px sits 5px above it.

         The BEHAVIOUR is unaffected — max(45, 40) and max(40, 40) are both
         symmetric — so the value is not being churned for 5px (1.6dp) on a
         3504px-wide panel that the owner has already looked at and accepted.
         Recorded here so the next person does not re-derive 45 from a density
         this device does not use. If it is ever retuned, 40px is the minimum
         that still buys symmetry at 320.

         ⚠️ Deliberately NOT tied to rounded_corner_radius above, even though
         they were briefly the same number and AOSP's own Pixel overlays do set
         them equal (pixel_7 is 47px radius / 47px padding, pixel_7_pro 50/50;
         PixelOS onyx is 14dp against a 156px radius). The radius is currently
         an unresolved experiment - see its comment - and coupling this to it
         would move the status bar every time the corner mask is probed, which
         is two variables at once. If the radius settles above 45px and the
         corners then look cramped, revisit; until then this stands on the dot
         floor alone.

         Keep the unit px for the same reason as the radius above: it tracks the
         glass, not the density.

         Second consumer, checked before setting this: SystemUI's
         navigation_layout.xml:22-23 and the framework's
         input_method_navigation_layout.xml:22-23 use it as the nav bar's
         start/end margin. On this device the nav bar is gesture mode, so that
         margin only insets the gesture pill's hit region by 20dp per side on a
         1557dp-wide bar - proportionally the same as any Pixel. -->
    <dimen name="rounded_corner_content_padding">45px</dimen>

    <!-- ★ 0.0 here was a black screen waiting to happen.
         DisplayDeviceConfig.java:2057-2071 reads this straight into
         mBacklightMinimum whenever it is not the -2 sentinel, and that is the
         floor below which the brightness slider cannot go. At 0.0 the slider
         bottoms out at backlight 0 on panel0-backlight, i.e. the panel is off.
         The comment above it ("brightnessMinimum 0.0") had copied stock's
         DisplayDeviceInfo.brightnessMinimum, which is in *brightness* space, not
         the resource's space.
         Correct value = BrightnessSynchronizer.brightnessIntToFloat(2)
                       = (2 - 1) / (255 - 1) = 1/254 = 0.003937008,
         derived from stock's config_screenBrightnessSettingMinimum=2 and
         confirmed against the running device: dumpsys power reports
         mScreenBrightnessMinimum=0.003937008.
         brightnessMaximum 1.0 / brightnessDefault 0.39763778 are unchanged. -->
    <item name="config_screenBrightnessSettingMinimumFloat" format="float" type="dimen">0.003937008</item>
    <item name="config_screenBrightnessSettingMaximumFloat" format="float" type="dimen">1.0</item>
    <item name="config_screenBrightnessSettingDefaultFloat" format="float" type="dimen">0.39763778</item>

    <!-- ⚠️ There used to be a config_screenBrightnessDozeFloat of 0.05 here.
         That was a misread of the dump: 0.05 is the *dim* value, not the doze
         value. Checked again on the running device:
             mScreenBrightnessDozeConfig=0.0
             mScreenBrightnessDimConfig=0.05
         Both already match AOSP's defaults (frameworks/base/core/res/res/
         values/config.xml: doze 0.0, dim 0.05), so neither needs an override
         and writing the dim value into the doze resource only made AOD wrong. -->

    <!-- ★ config_mainDisplayShape is deliberately NOT carried over from the
         stock vendor RRO, even though that RRO does set it. Decoded, stock's
         value is

             M20 0 h1040 s20 0 20 20 v2360 s0 20 -20 20 h-1040 s-20 0 -20 -20 v-2360 ...

         i.e. a rounded rectangle 20+1040+20 = 1080 wide and 20+2360+20 = 2400
         tall. This panel is 2190x3504. The path is for some other model that
         shares Lenovo's vendor image — the same pattern as the
         vendor.lenovo.hardware.vibrator_dual-service entry in stock's
         file_contexts, whose binary does not exist on this device either.

         Copying it would tell every app calling WindowInsets#getDisplayShape()
         that the screen is 1080x2400. AOSP's default is the empty string, which
         makes DisplayShape.fromResources() synthesise the shape from the real
         display size and the rounded_corner_radius above — correct here, and
         correct on any future panel. Leaving it unset is the right answer, not
         an omission. -->

    <!-- No config_mainBuiltInDisplayCutout override: the panel has no cutout and
         AOSP's default for that resource is already the empty string
         (frameworks/base/core/res/res/values/config.xml). Restating it added
         nothing, and this file's stated policy is to record measured
         differences, not to copy framework-res. -->

    <!-- The device has no notification LED; that is the AOSP default already,
         so it is not restated here. Add entries only when the stock dump
         disagrees with AOSP, so this file stays a record of measured
         differences rather than a copy of framework-res. -->

    <!-- ⚠️ This block used to also claim that "no physical navigation keys" was
         an AOSP default needing no entry. That reasoning was backwards and
         would have shipped an unnavigable device.

         AOSP's default is false (frameworks/base/core/res/res/values/config.xml
         :2772), and DisplayPolicy.java:677-688 reads it to decide whether
         SystemUI creates a NavigationBar at all. false means no Home/Back/
         Recents AND no edge-back gesture handler.

         The device really does have no navigation keys — `getevent -pl` finds
         only gpio-keys VOLUMEUP, pmic_resin VOLUMEDOWN and pmic_pwrkey POWER —
         which is precisely why the bar has to be turned ON.

         Stock sets it true from product/overlay/LgsiFrameworksOverlay.apk, and
         LineageOS replaces the whole product partition, so that RRO is gone.
         Without this the device boots to Launcher and cannot be operated
         without the folio keyboard. onyx's overlay sets it too. -->
    <bool name="config_showNavigationBar">true</bool>

    <!-- Wired headset detect goes through input events, not the audio HAL.
         AOSP default is false (config.xml:3465); stock sets true in
         vendor/overlay/FrameworksResTarget_Vendor.apk, which we also drop.
         Confirmed on the device: event5 "tuna-qrd-snd-card Headset Jack"
         reports SW_HEADPHONE_INSERT / SW_MICROPHONE_INSERT / SW_LINEOUT_INSERT
         and event6 "Button Jack" reports KEY_MEDIA / KEY_VOLUME{UP,DOWN}.
         Without this, plug/unplug and inline buttons do nothing. -->
    <bool name="config_useDevInputEventForAudioJack">true</bool>

    <!-- The QTI power stack expects auto-suspend to be decoupled from the
         display. AOSP default is false (config.xml:3028); stock sets true, and
         the running device confirms it: `dumpsys power` shows
         mDecoupleHalAutoSuspendModeFromDisplayConfig=true and
         mHalAutoSuspendModeEnabled=true. onyx sets it as well. -->
    <bool name="config_powerDecoupleAutoSuspendModeFromDisplay">true</bool>
    <!-- ================================================================== -->
    <!-- NEW: adaptive brightness                                           -->
    <!-- ================================================================== -->

    <!-- Flag indicating whether we should enable the automatic brightness.
         Software implementation will be used if config_hardware_auto_brightness_available is not set -->
    <bool name="config_automatic_brightness_available">true</bool>

    <!-- Array of light sensor lux values to define our levels for auto backlight brightness support.
         The N entries of this array define N + 1 control points as follows:
         (1-based arrays)

         Point 1:            (0, value[1]):             lux <= 0
         Point 2:     (level[1], value[2]):  0        < lux <= level[1]
         Point 3:     (level[2], value[3]):  level[2] < lux <= level[3]
         ...
         Point N+1: (level[N], value[N+1]):  level[N] < lux

         The control points must be strictly increasing.  Each control point
         corresponds to an entry in the brightness backlight values arrays.
         For example, if lux == level[1] (first element of the levels array)
         then the brightness will be determined by value[2] (second element
         of the brightness values array).

         Spline interpolation is used to determine the auto-brightness
         backlight values for lux levels between these control points.

         Must be overridden in platform specific overlays -->
    <integer-array name="config_autoBrightnessLevels">
        <item>5</item>
        <item>15</item>
        <item>40</item>
        <item>100</item>
        <item>180</item>
        <item>400</item>
        <item>550</item>
        <item>1000</item>
        <item>1500</item>
        <item>2000</item>
        <item>2500</item>
        <item>3000</item>
        <item>4000</item>
        <item>5000</item>
    </integer-array>

    <!-- Array of desired screen brightness in nits corresponding to the lux values
         in the config_autoBrightnessLevels array. The display brightness is defined as the measured
         brightness of an all-white image.

         If this is defined then:
            - config_autoBrightnessLcdBacklightValues should not be defined
            - config_screenBrightnessNits must be defined
            - config_screenBrightnessBacklight must be defined

         This array should have size one greater than the size of the config_autoBrightnessLevels
         array. The brightness values must be non-negative and non-decreasing. This must be
         overridden in platform specific overlays -->
    <array name="config_autoBrightnessDisplayValuesNits">
        <item>8</item>
        <item>8</item>
        <item>30</item>
        <item>70</item>
        <item>100</item>
        <item>120</item>
        <item>150</item>
        <item>180</item>
        <item>210</item>
        <item>270</item>
        <item>340</item>
        <item>420</item>
        <item>480</item>
        <item>550</item>
        <item>650</item>
    </array>

    <!-- An array describing the screen's backlight values corresponding to the brightness
         values in the config_screenBrightnessNits array.

         This array should be equal in size to config_screenBrightnessBacklight. -->
    <integer-array name="config_screenBrightnessBacklight">
        <item>0</item>
        <item>4</item>
        <item>13</item>
        <item>30</item>
        <item>43</item>
        <item>51</item>
        <item>64</item>
        <item>77</item>
        <item>90</item>
        <item>115</item>
        <item>145</item>
        <item>179</item>
        <item>205</item>
        <item>235</item>
        <item>255</item>
    </integer-array>

    <!-- An array of floats describing the screen brightness in nits corresponding to the backlight
         values in the config_screenBrightnessBacklight array.  On OLED displays these  values
         should be measured with an all white image while the display is in the fully on state.
         Note that this value should *not* reflect the maximum brightness value for any high
         brightness modes but only the maximum brightness value obtainable in a sustainable manner.

         This array should be equal in size to config_screenBrightnessBacklight -->
    <array name="config_screenBrightnessNits">
        <item>0</item>
        <item>8</item>
        <item>30</item>
        <item>70</item>
        <item>100</item>
        <item>120</item>
        <item>150</item>
        <item>180</item>
        <item>210</item>
        <item>270</item>
        <item>340</item>
        <item>420</item>
        <item>480</item>
        <item>550</item>
        <item>650</item>
    </array>

    <!-- ⚠️ config_autoBrightness{Brightening,Darkening}LightDebounce were set here
         to 2000/3000 and are GONE, because on this path they are dead resources.
         The live values now live in
         configs/displayconfig/display_port_147.xml, installed to
         /vendor/etc/displayconfig/.

         They read back correctly — `cmd overlay lookup android
         android:integer/config_autoBrightnessBrighteningLightDebounce` on the
         device returned 2000, which is exactly why this looked done — and yet
         `dumpsys display` reported the debounce as -1. The reason is that the
         only caller of loadAutoBrightnessBrighteningLightDebounce() is
         loadAutoBrightnessConfigValues() (DisplayDeviceConfig.java:2467-2472),
         which runs from initFromFile() alone. Without a DisplayDeviceConfig XML
         the device takes initFromGlobalXml() (:1947-1959), which calls
         loadAutoBrightnessConfigsFromConfigXml() — that builds the brightness
         mapping and nothing else. The resource fallback inside the loader is for
         a DDC file that omits the tag; it is not a config.xml path.

         Keeping them here as well would be two places that must agree for no
         benefit, since this copy can never win and can never be reached.
         This is the same shape as the tree's other "written but not reachable"
         findings; a resource can resolve correctly in an overlay lookup and
         still be bypassed by the framework code path. -->


    <!-- Note: This setting is deprecated, please use
    config_screenBrightnessSettingMinimumFloat instead -->
    <!-- Kept in sync with the float above (2 -> 1/254). PowerManager.java:1401-1403
         exposes this int directly via getMinimumScreenBrightnessSetting(), and it
         is the fallback DisplayDeviceConfig uses if the float ever reverts to -2. -->
    <integer name="config_screenBrightnessSettingMinimum">2</integer>

    <!-- Screen brightness used to dim the screen while dozing in a very low power state.
         May be less than the minimum allowed brightness setting
         that can be set by the user. -->
    <!-- Set the int only, not config_screenBrightnessDozeFloat: AOSP's float default
         is 0.0 rather than the -2 sentinel, so DisplayDeviceConfig never falls back
         to this int. It is SystemUI that consumes it, at
         DozeParameters.java:209 (value / 255f). Stock uses 17. -->
    <integer name="config_screenBrightnessDoze">17</integer>

    <!-- ================================================================== -->
    <!-- Folio cover: close to sleep, open to wake                          -->
    <!-- ================================================================== -->

    <!-- Paired with configs/device_state_configuration.xml, which defines
         state 0 = OPENED and state 1 = CLOSED from the BU52053NVX hall sensor.
         That file carries the evidence for the mapping and the polarity.

         Both these arrays AND the PROPERTY_POWER_CONFIGURATION_TRIGGER_*
         properties in the XML are required, because LogicalDisplayMapper
         branches on a feature flag and reads a different one on each side:
         shouldDeviceBeWoken / shouldDeviceBePutToSleep (:642-700) use the
         properties when mDeviceStateManagerFlags.deviceStatePropertyMigration()
         is on and these arrays when it is not. Setting only one half works
         until the flag flips.

         AOSP's defaults are both empty (core/res/res/values/config.xml:731,740),
         which is why the folio does nothing today. -->
    <integer-array name="config_deviceStatesOnWhichToWakeUp">
        <item>0</item>
    </integer-array>
    <integer-array name="config_deviceStatesOnWhichToSleep">
        <item>1</item>
    </integer-array>

    <!-- ================================================================== -->
    <!-- NEW: PinnerService                                                 -->
    <!-- ================================================================== -->

    <!-- Default files to pin via Pinner Service -->
    <!-- Stock's list has 9 entries. Two of them, /system/framework/arm64/
         boot-QPerformance.oat and boot-UxPerformance.oat, are QTI perf JARs that
         LineageOS does not build either (QPerformance/UxPerformance appear in no
         .mk or .bp in the tree; the "verified absent from out/" evidence was
         gathered on a PixelOS build and wants re-checking after a flash),
         so they are dropped. The remaining 7 were each confirmed present in the
         build tree. -->
    <string-array translatable="false" name="config_defaultPinnerServiceFiles">
        <item>/system/framework/framework.jar</item>
        <item>/system/framework/services.jar</item>
        <item>/apex/com.android.media/javalib/updatable-media.jar</item>
        <item>/system/lib64/libhwui.so</item>
        <item>/vendor/lib64/libllvm-qgl.so</item>
        <item>/vendor/lib64/libllvm-glnext.so</item>
        <item>/vendor/lib64/libsdmextension.so</item>
    </string-array>

    <!-- ================================================================== -->
    <!-- NEW: power behaviour (stock parity, optional)                      -->
    <!-- ================================================================== -->

    <!-- If this is true, the screen will come on when you unplug usb/power/whatever. -->
    <bool name="config_unplugTurnsOnScreen">true</bool>

    <!-- ================================================================== -->
    <!-- Doze / Always-On Display                                           -->
    <!-- ================================================================== -->

    <!-- Without this the entire doze subsystem is inert, and the omission is
         silent. AOSP's default is the empty string
         (frameworks/base/core/res/res/values/config.xml:2933) and nothing in
         vendor/, device/ or packages/ sets it. The chain:
         AmbientDisplayConfiguration.ambientDisplayAvailable() is
         !isEmpty(config_dozeComponent) -> false, so
         pulseOnNotificationAvailable() is false and every doze feature is
         disabled; DreamManagerService then gets a null ComponentName and
         DozeService is never bound.

         This tree was already self-inconsistent about it: config_screenBrightnessDoze
         above cites DozeParameters.java, which is code inside DozeService,
         a service that never ran. Both stock RROs decode to exactly this value,
         and so do both peer trees. -->
    <string name="config_dozeComponent" translatable="false">com.android.systemui/com.android.systemui.doze.DozeService</string>

    <!-- Screen saver off by default. AOSP ships true
         (core/res/res/values/config.xml:2826) and nothing in
         vendor/lineage/overlay/** changes it, so without this line the tablet
         offers a screen saver where stock does not. Stock forces false in
         product/overlay/LgsiFrameworksOverlay.apk.

         ⚠️ This is the *user-facing* screen saver, not doze. It does not touch
         config_dozeComponent above: DreamManagerService still binds DozeService
         for ambient display — verified live, `dumpsys dreams` reports
         mDreamsSupportedConfig=true with DozeService as the active dream while
         the screen is off. Only the "Screen saver" Settings default changes.

         Owner decision, session 23: match stock. -->
    <bool name="config_dreamsEnabledByDefault">false</bool>

    <!-- ================================================================== -->
    <!-- Colour modes                                                       -->
    <!-- ================================================================== -->

    <!-- AOSP's default is an EMPTY array (core/res/res/values/config.xml:1079-1085),
         which makes ColorDisplayService.isColorModeAvailable() always false and
         leaves colorMode at NOT_SET — so Settings > Display > Colors vanishes
         entirely. So an override is needed; the question is which values.

         The four AOSP modes, and only those:
             0 NATURAL   1 BOOSTED   2 SATURATED   3 AUTOMATIC (shown as "Adaptive")

         ⚠️ This used to also list the ten vendor-range modes 256-265
         (VENDOR_COLOR_MODE_RANGE_MIN = 256), copied from stock. The owner
         reported the symptom precisely: those entries appear in Settings with
         NO NAME, and selecting one does nothing. Both halves are explained, and
         neither is fixable from a device overlay:

           - No name. Settings takes the list of modes from
             com.android.internal.R.array.config_availableColorModes
             (ColorModeUtils.kt:64) but takes the NAMES from its own
             Settings/res/values/config.xml:625-640, which defines exactly four
             values: 0,1,2,3. getColorModeMapping() returns a map built from
             those, so colorModesToSummaries.get(256) is null and
             ColorModePreferenceFragment.getCandidates() adds a candidate with a
             null title.
           - No effect. DisplayTransformManager.java:282-286 passes a vendor
             mode straight through to setDisplayColor(colorMode), which is
             SurfaceFlinger binder transaction 1023, and
             SurfaceFlinger.cpp:7255-7256 does
                 mDisplayColorSetting = static_cast<DisplayColorSetting>(value)
             DisplayColorSetting only has MANAGED/UNMANAGED/ENHANCED, so 256-265
             cast to an out-of-range value that no branch matches. These modes
             exist only in QTI's SurfaceFlinger fork
             (libsurfaceflingerextension), and LineageOS builds AOSP's —
             libsurfaceflingerextension appears nowhere in this tree.
             Independently confirmed on the running device: SurfaceFlinger
             reports exactly three supported colour modes — NATIVE, SRGB,
             DISPLAY_P3 — with no vendor entries at all.

         2 (SATURATED) is ADDED, having been missing before. It is the one mode
         with a guaranteed visible effect: DisplayTransformManager maps it to
         DISPLAY_COLOR_UNMANAGED, i.e. render straight to the panel's native
         gamut with no colour management. -->
    <integer-array name="config_availableColorModes">
        <item>0</item>
        <item>1</item>
        <item>2</item>
        <item>3</item>
    </integer-array>

    <!-- Three values the factory ROM sets through RROs on the *product*
         partition, which LineageOS replaces. Nothing warns when they disappear:
         each one silently falls back to the AOSP default and the feature just
         behaves differently. Recovered by dumping every stock RRO that targets
         `android` with aapt2 and diffing against the built framework-res.apk
         (which already includes the PixelOS/LineageOS static overlays). -->

    <!-- ⚠️ SESSION 33 — THIS LINE DOES NOT DO WHAT THE COMMENT SAID, AND THE
         COMMENT SAID THE OPPOSITE OF WHAT THE VALUE MEANS.

         It used to read *"Device owner's decision, settled: no user switcher on
         the lock screen … Do not flip it back."* Both halves were wrong:

           · It changes NOTHING. AOSP already ships false in BOTH
             core/res/res/values/config.xml and values-sw600dp/config.xml, and
             LineageOS repeats it. Measured:
             `cmd overlay lookup android android:bool/config_keyguardUserSwitcher`
             returns false with or without this line.
           · false is the value that SELECTS the avatar, not the value that
             removes it. SystemUI's own flags.xml says the icon "is only shown if
             config_keyguardUserSwitcher=false"; true swaps the small avatar for a
             full on-keyguard user LIST. So "flipping it back" would have made the
             lock screen busier, not cleaner.

         The line stays, because restating a device requirement rather than
         depending on an upstream default is this tree's convention — the same
         argument the refresh-rate overlay makes for its three bools. What is
         removed is the claim that it implements the owner's decision. The thing
         that implements it is the next resource down. -->
    <bool name="config_keyguardUserSwitcher">false</bool>

    <!-- ★ Device owner's decision, settled: no user-switcher avatar in the
         top-right of the lock screen.

         THIS is the resource that does it. The view is @+id/multi_user_avatar
         (SystemUI res/layout/keyguard_status_bar.xml:62-66), and the chain that
         made it visible is entirely a screen-size accident:

           this device reports sw1095dp, so every -sw600dp qualifier applies
             -> core/res/res/values-sw600dp/config.xml:53 sets
                config_showUserSwitcherByDefault = TRUE
                (values/config.xml:6357 is false; symbols.xml:4916)
             -> that is the DEFAULT behind Settings.Global.USER_SWITCHER_ENABLED,
                which is unset here
             -> UserManagerService.isUserSwitcherEnabled() -> true
             -> KeyguardStatusBarView.updateVisibilities() -> VISIBLE

         A bare values/ entry here overrides the target's -sw600dp variant, which
         is how every other size-qualified value in this file is overridden.

         ★ VERIFIED ON THE DEVICE, and the first measurement looked like a
         refutation: writing `settings put global user_switcher_enabled 0` changed
         nothing in the view hierarchy. That is because the controller reads the
         value when it is constructed rather than observing it. After
         `killall com.android.systemui` the same dump reads
             ImageView{… G.ED… 0,0-0,0 … app:id/multi_user_avatar}
         — GONE. The gate is real; only the instrument was wrong. (HANDOFF lesson
         7: before believing an experiment failed, check the thing you measured
         could have changed.)

         ⚠️ This is a DEFAULT, not a removal, and that is deliberate. Settings >
         System > Multiple users writes USER_SWITCHER_ENABLED directly
         (MultiUserMainSwitchPreferenceController.java:63), and its toggle is
         still shown because config_allowChangeUserSwitcherEnabled is true, so the
         owner can turn it back on and it will override this.

         ⚠️ Do NOT reach for config_multiuserMaximumUsers = 1 instead. That makes
         UserManager.supportsMultipleUsers() false, which deletes the whole
         Multiple users page and guest mode — far more than was asked for.

         ★ ⚠️ AND THIS LINE ALONE DOES NOTHING. The target declares both of these
         bools in values-sw600dp/ as well, this device is sw1095dp, and a
         target's more specific configuration OUT-MATCHES an overlay's default
         one — the value here is found and then never consulted. Both names are
         restated in ../values-sw600dp/config.xml, which is what actually takes
         effect, and that file's header has the measurement, the audit of every
         other resource in this tree's six overlays, and the one-line test:
         `adb shell cmd overlay lookup`, in verbose mode, against `android` and
         `android:bool/config_showUserSwitcherByDefault`, must print an
         "Overlaid:" line. "Found initial:" on its own is the failure.

         This was caught by flashing and looking, not by reading. -->
    <bool name="config_showUserSwitcherByDefault">false</bool>

    <!-- Stock: true, from product/overlay/FrameworksResCommon_Sys.apk.
         AudioManager.java:7054 turns this into
         PROPERTY_SUPPORT_AUDIO_SOURCE_UNPROCESSED. The QTI HAL does support the
         unprocessed source, so reporting the AOSP default of false tells
         recording apps a capability the hardware has is unavailable. -->
    <bool name="config_supportAudioSourceUnprocessed">true</bool>

    <!-- Stock: 2500 / 4628 / 3420 (LgsiFrameworksOverlay). AOSP defaults are
         2596 / 4082 / 2850. These are Night Light's colour-temperature range and
         default; the stock triple is calibrated for this panel. Night Light is
         genuinely reachable here — config_setColorTransformAccelerated is
         already true via vendor/lineage/overlay/common/frameworks/base/core/res/
         res/values/config.xml:157 — so these are live, not dead keys. -->
    <integer name="config_nightDisplayColorTemperatureMin">2500</integer>
    <integer name="config_nightDisplayColorTemperatureMax">4628</integer>
    <integer name="config_nightDisplayColorTemperatureDefault">3420</integer>

    <!-- ★ The other half of the same stock RRO, added session 23. Carrying the
         three integers above without these two arrays meant the shipped default
         of 3420 K produced a DIFFERENT colour from stock's 3420 K, which makes
         "calibrated for this panel" untrue of the thing that was carried.

         Eight of the nine terms are AOSP's verbatim. The ninth, the blue
         y-intercept, is Lenovo's: -0.298650895 against AOSP's -0.198650895
         (frameworks/base/core/res/res/values/config.xml:951 and :960; stock's
         values read out of product/overlay/LgsiFrameworksOverlay.apk with
         aapt2 dump resources). At 3420 K that is roughly 0.614 blue on AOSP's
         curve against 0.514 on Lenovo's — i.e. stock's Night Light is visibly
         warmer at the same nominal temperature, and this is what the panel was
         tuned against.

         ColorDisplayService.java:1458-1459 picks between the two arrays by
         colour mode, so both have to be set or the difference appears and
         disappears when the user changes screen colours. Stock sets them
         identically; so do we.

         Owner decision, session 23: match stock. -->
    <string-array name="config_nightDisplayColorTemperatureCoefficients">
        <!-- R a-coefficient --> <item>0.0</item>
        <!-- R b-coefficient --> <item>0.0</item>
        <!-- R y-intercept --> <item>1.0</item>
        <!-- G a-coefficient --> <item>-0.00000000962353339</item>
        <!-- G b-coefficient --> <item>0.000153045476</item>
        <!-- G y-intercept --> <item>0.390782778</item>
        <!-- B a-coefficient --> <item>-0.0000000189359041</item>
        <!-- B b-coefficient --> <item>0.000302412211</item>
        <!-- B y-intercept --> <item>-0.298650895</item>
    </string-array>

    <string-array name="config_nightDisplayColorTemperatureCoefficientsNative">
        <!-- R a-coefficient --> <item>0.0</item>
        <!-- R b-coefficient --> <item>0.0</item>
        <!-- R y-intercept --> <item>1.0</item>
        <!-- G a-coefficient --> <item>-0.00000000962353339</item>
        <!-- G b-coefficient --> <item>0.000153045476</item>
        <!-- G y-intercept --> <item>0.390782778</item>
        <!-- B a-coefficient --> <item>-0.0000000189359041</item>
        <!-- B b-coefficient --> <item>0.000302412211</item>
        <!-- B y-intercept --> <item>-0.298650895</item>
    </string-array>

    <!-- Miracast / Wi-Fi Display. AOSP default is false
         (core/res/res/values/config.xml:3425) and
         DisplayManagerService.java:2397-2405 only builds a WifiDisplayAdapter
         when it is true, so without this the whole vendor stack this tree
         already ships is unreachable: wfdservice64, WfdService.apk and
         libwfdnative.so are extracted, device.mk installs libinput_shim
         specifically so libwfdnative resolves MotionEvent::initialize on
         Android 16, and libwfdaac_vendor is in PRODUCT_PACKAGES. Stock turns it
         on with persist.debug.wfd.enable=1 in system/build.prop:142 — the
         partition LineageOS replaces, which is how the switch got lost. The
         matching vendor-side properties are in properties/system.prop.

         The QTI integration point exists in this tree: WifiDisplayController
         holds mExtRemoteDisplay and dispatches through
         ExtendedRemoteDisplayHelper (:767,:775), which is what WfdService.apk
         registers with; AOSP's own RemoteDisplay path is the fallback and is
         also still present (frameworks/av/media/libstagefright/wifi-display).

         ⚠️ Not yet exercised against a real sink. If casting turns out not to
         work, set this back to false and drop the five blobs rather than
         leaving a Settings entry that fails — an option that is guaranteed to
         fail is worse than an absent one. -->
    <bool name="config_enableWifiDisplay">true</bool>

    <!-- ★ Added session 23, and it belongs with the line above rather than on
         its own: turning Miracast on without it means the cast display cannot
         carry protected content.

         WifiDisplayAdapter.java:116-117 -> :408 is the only place
         FLAG_SUPPORTS_PROTECTED_BUFFERS is set on the cast virtual display, and
         it reads exactly this resource. AOSP defaults it false
         (core/res/res/values/config.xml:3494); stock sets it true in
         product/overlay/FrameworksResCommon_Sys.apk. The hardware agrees:
         `dumpsys wifi` reports WIFI_FEATURE_WFD_R2, and this tree already ships
         the HDCP half of the stack (android.hardware.drm-service.wfdhdcp,
         registered live as IDrmFactory/wfdhdcp).

         The same caveat as config_enableWifiDisplay applies — untested against a
         real sink — but the failure modes differ: without this, DRM video
         silently casts as a black rectangle rather than not casting at all,
         which is the harder one to diagnose. -->
    <bool name="config_wifiDisplaySupportsProtectedBuffers">true</bool>

    <!-- ================================================================== -->
    <!-- Desktop windowing: off                                             -->
    <!-- ================================================================== -->

    <!-- Attaching the folio keyboard put every app into a freeform desktop
         window. That is not this tree's choice and it is not AOSP's default
         either: core/res/res/values/config.xml:7559,7562 ship both of these as
         false.

         ONE of them is turned on underneath us by an inherited STATIC overlay,
         which is why it does not appear anywhere in this device tree:

             vendor/lineage/overlay/common/.../config.xml:90
                 config_isDesktopModeSupported        -> true   (every device)

         ⚠️ The second line here used to say that
         "vendor/custom/overlay/tablet/.../config.xml, line 19" sets
         config_canInternalDisplayHostDesktops true for every tablet — and that
         is a PixelOS path which does not exist on LineageOS.
         (Written without the colon-number form on purpose: it is a dead
         citation, and work/scripts/34-citation-check.py would otherwise try to
         resolve it forever.)
         vendor/lineage/overlay/tablet/ contains only two files, both about the
         glanceable hub, and neither touches this resource — so
         config_canInternalDisplayHostDesktops is already false from AOSP and the
         override below is REDUNDANT. It is kept anyway, for the reason stated
         further down: the intent is "this tablet does not host desktops", and
         stating it once leaves the next reader wondering whether the other half
         was missed. Redundant and explicit beats absent and inferred.

         Static overlays are baked into framework-res.apk, so an RRO always wins
         over them — no partition-order question here.
         Verified on the device before writing this: `cmd overlay lookup` in
         verbose mode reported both resolving from framework-res.apk itself with
         no competing RRO in the chain, while config_showNavigationBar on the
         same run showed "Overlaid: /vendor/overlay/FrameworkOverlayMalbec.apk".
         (Spelling the flag out is not an option here: a double hyphen is illegal
         inside an XML comment and aapt2 only reports it about 73% into a build.)

         The gate is DesktopStateImpl.kt:53-59:

             isDeviceEligibleForDesktopModeDevOption =
                 if (!enforceDeviceRestrictions) true
                 else (isDesktopModeSupported && canInternalDisplayHostDesktops)
                      || isDesktopModeDevOptionSupported

         enforceDeviceRestrictions comes from
         persist.wm.debug.desktop_mode_enforce_device_restrictions, unset here, so
         it defaults true and the two resources below are live. The third term,
         config_isDesktopModeDevOptionSupported, is false in AOSP and nothing in
         this tree or its inherited overlays sets it — checked, not assumed.

         Both are written even though the expression is an AND and one would do:
         the intent is "this tablet does not host desktops", and stating it once
         leaves the next reader wondering whether the other half was missed.

         ⚠️ Known cost, accepted by the owner: desktop windows on an external
         display go away too. This is a Wi-Fi-only tablet with no wired display
         output in use. -->
    <bool name="config_isDesktopModeSupported">false</bool>
    <bool name="config_canInternalDisplayHostDesktops">false</bool>

    <!-- ★ The trusted certificate for downloadable font updates.
         WITHOUT THIS, GMS FONT UPDATES HAVE NEVER WORKED AND NEVER COULD.

         Every boot, GMS pushes three families and all three are rejected:

             E FontManagerService: SystemFontException: Font validation failed.
               Fs-verity is not enabled: /data/fonts/files/…/GoogleSans-Regular.ttf
                                         …/GoogleSansFlex-Regular.ttf
                                         …/GoogleSansText-Regular.ttf

         ⚠️ The message names the wrong cause and the first diagnosis followed
         it. UpdatableFontDir.java:570 throws that string when
         isFromTrustedProvider() returns false, and that method has TWO failure
         paths (FontManagerService.java:189-205): a null fs-verity digest, or a
         PKCS#7 signature matching no trusted cert. It is not the digest:

           * CONFIG_FS_VERITY=y in this kernel and /proc/sys/fs/verity/ exists
           * /data is f2fs with the `verity` feature (/sys/fs/f2fs/dm-45/features)
           * `grep -c "Failed to get fs-verity digest"` over a whole 16 hour
             buffer is 0, and that warning is logged unconditionally on the
             digest path

         ★ So the signature check is what fails, and the reason is upstream:
         AOSP ships config_fontManagerServiceCerts as an EMPTY string-array
         (core/res/res/values/config.xml:7358). mDerCertPaths is therefore empty,
         the `for (String certPath : mDerCertPaths)` loop at :195 never executes,
         and isFromTrustedProvider returns false unconditionally. The check
         cannot pass on any build that does not override this resource. That is
         an unfalsifiable check one layer up, in AOSP.

         ★★ And the certificate was already on the device, referenced by nothing.
         MindTheGapps installs it
         (vendor/gapps/common/proprietary/product/etc/security/fsverity/):

             /product/etc/security/fsverity/gms_fsverity_cert.der   1484 B
             C=US, O=Google Inc., OU=Android, CN=Android
             notBefore 2021-02-23   notAfter 2051-02-23

         Stock ZUI ships the identical cert at the identical path and wires it
         up. This is one of the few places stock is simply right and we were not.

         ⚠️ Verify with `cmd overlay lookup android android:array/
         config_fontManagerServiceCerts` and require the "Overlaid:" line, not
         just "Found initial:". Acceptance: the three FontManagerService errors
         stop, and `cmd font dump` reports the updated families.

         ⚠️ Stock also ships product/etc/security/fsverity/play_store_fsi_cert.der
         (Google Android Integrity Signing Certificate), which MindTheGapps does
         not. No consumer for it was found in this tree. It is NOT part of this
         fix and must not be added on symmetry alone. -->
    <string-array name="config_fontManagerServiceCerts" translatable="false">
        <item>/product/etc/security/fsverity/gms_fsverity_cert.der</item>
    </string-array>

    <!-- ★ Emergency SOS, off. Owner's decision, session 34.

         Five presses of the power button arm an emergency countdown. On this
         tablet that countdown can never place a call, and the user cannot turn
         it off. All four halves measured on the running device:

             config_emergencyGestureEnabled          true   (feature active)
             config_defaultEmergencyGestureEnabled   true   (default ON)
             Settings.Secure.EMERGENCY_GESTURE_ENABLED  unset, so the default wins
             the Settings page that would disable it     GONE

         GestureLauncherService.java:614-620 reads the first two, and :626-635
         keeps the whole service alive on the strength of them.

         ★ The page is gone because LineageOS itself removed it for every
         Wi-Fi-only product: vendor/lineage/overlay/wifionly/packages/apps/
         Settings/res/values/config.xml:25 sets config_show_emergency_gesture_settings
         false. Measured: `am start -a com.android.settings.action.EMERGENCY_GESTURE_SETTINGS`
         returns "unable to resolve Intent".

         So upstream decided the SETTING makes no sense without a modem and hid
         it, and nothing turned the FEATURE off. The port inherited an armed
         gesture with its own off switch removed, which is a state nobody chose.

         ⚠️ The trade, stated: the gesture can still sound an alarm and share
         location without placing a call. The owner weighed that against a
         countdown a user cannot stop reaching and chose off. This device has no
         android.hardware.telephony feature of any kind (device.mk omits them
         all) and no carrier_config service. -->
    <bool name="config_emergencyGestureEnabled">false</bool>

    <!-- ★ Face-down screen off, off. Owner's decision, session 34.

         A phone laid face down on a desk is a deliberate "be quiet" gesture. A
         13 inch tablet lying flat in a folio is how tablets sit.

         This resource is the ONLY brake. FaceDownDetector.java:345-349 reads it;
         the DeviceConfig gate defaults on (:74,:77) and mEnabledOverride
         defaults true (:81), so nothing else stops it. Measured before the
         change: `dumpsys power` reported FaceDownDetector mActive=true, i.e. the
         accelerometer listener was genuinely running, not merely compiled in.

         Acceptance is the same dump reading mActive=false, plus laying the
         tablet flat and watching the screen stay on. -->
    <bool name="config_flipToScreenOffEnabled">false</bool>

    <!-- ★ Letterbox reachability, on. Owner's decision, session 34.

         A portrait only app on a 1752 dp wide screen is pillarboxed with very
         wide dead margins and, by AOSP default, welded to the centre of them.
         All three reachability resources read false on this build, measured with
         `cmd overlay lookup`: horizontal, vertical, and the education overlay
         that teaches the gesture. Pixel Tablet ships horizontal on; stock ZUI
         ships none of it.

         AppCompatConfiguration.java:365-366 reads the horizontal bool;
         AppCompatReachabilityOverrides.java:269-288 gates the behaviour on
         fullscreen plus horizontally pillarboxed; AppCompatReachabilityPolicy.java:138
         consumes it. Double tapping either side then moves the app to that edge.

         config_letterboxIsEducationEnabled ships with it deliberately
         (AppCompatConfiguration.java:376). Without the one time overlay the
         gesture is undiscoverable, and an invisible feature is not a feature.

         ⚠️ Vertical reachability is deliberately NOT enabled. It applies to
         landscape only apps in portrait, which on a landscape primary tablet is
         the rarer case, and each of these is a distinct behaviour to verify.

         Scope: this affects only fullscreen activities that AOSP has already
         pillarboxed. It does not make a resizable app non-resizable or replace
         ordinary split-screen and Activity Embedding behaviour. -->
    <bool name="config_letterboxIsHorizontalReachabilityEnabled">true</bool>
    <bool name="config_letterboxIsEducationEnabled">true</bool>

    <!-- ★★ The DeviceConfig writer. Owner's decision, session 35, taken twice:
         once on a premise that turned out to be wrong, and again with the
         corrected one in front of them.

         The symptom is 182 lines per boot, from com.google.android.gms.persistent:

             W PlatformConfigurator: setProperties failed with SecurityException
             E DatabaseUtils: java.lang.SecurityException: Permission denial to
                 mutate flag, must have root, WRITE_DEVICE_CONFIG, or
                 WRITE_ALLOWLISTED_DEVICE_CONFIG

         So every Phenotype flag Google pushes for a PLATFORM namespace is
         rejected, and every AOSP feature Google gates server side sits at its
         build time default. The chain, all four links read rather than assumed:

             core/res/AndroidManifest.xml:4951-4952
                 WRITE_DEVICE_CONFIG is signature|verifier|configurator
             services/permission/.../permission/Permission.kt:75-76
                 isConfigurator = protectionFlags has PROTECTION_FLAG_CONFIGURATOR
             services/permission/.../permission/AppIdPermissionPolicy.kt:1737-1738
                 grants it only to knownPackages[KnownPackages.PACKAGE_CONFIGURATOR]
             services/core/.../pm/PackageManagerService.java:2322
                 which is getString(R.string.config_deviceConfiguratorPackageName)

         WRITE_ALLOWLISTED_DEVICE_CONFIG is not a second door: AndroidManifest.xml
         :4956-4957 gives it the identical protection level, so it resolves to the
         identical single package. There is exactly ONE slot.

         ⚠️⚠️ THE TRAP, and it cost this session a wrong answer before
         `cmd overlay lookup` in its verbose form caught it. AOSP ships this resource EMPTY
         (core/res/res/values/config.xml:4029), which reads like an unfilled slot
         and invites the conclusion that naming GMS here costs nothing. It is not
         empty on a LineageOS build. Upstream fills it at
         vendor/lineage/overlay/common/frameworks/base/core/res/res/values/config.xml:118
         with org.protonaosp.deviceconfig, which really ships here
         (/system_ext/priv-app/SimpleDeviceConfig/, installed, permission granted).
         So this line does not fill a slot, it TAKES one. Fact 37 is why that was
         caught: read the value back on the device, never out of AOSP's values/.
         ⚠️ And the verbose form is required — the plain lookup prints only the
         winning value, not who lost.

         ★★ WHAT IT COSTS: eight flags, and it took THREE wrong answers to find
         out which eight and why. The sequence is the useful part.

         The obvious worry is that displacing LineageOS's package silences it.
         SimpleDeviceConfig's BootReceiver.java:29-48 replays a string-array on
         every BOOT_COMPLETED with makeDefault=true, and upstream's payload for it
         is 8 real flags: privacy indicators x2, the privacy dashboard's app-op
         list, attention keep_screen_on, the MediaProvider cloud-picker allowlist,
         the autofill browser-compat list, and two SeedVault transport timeouts.
         None of those is free on this device — three have hardcoded defaults with
         no resource behind them, autofill falls back to a Settings.Global with no
         def_ entry, and SeedVault really ships here (com.stevesoltys.seedvault).

         ⚠️ AND THE WORRY IS WRONG, because the configurator slot was never what
         granted SimpleDeviceConfig the permission. WRITE_DEVICE_CONFIG is
         signature|verifier|configurator, three flags, and the incumbent satisfies
         the FIRST one: measured on the device, SimpleDeviceConfig's signature is
         b4addb29 and the `android` platform package's signature is b4addb29 — the
         same key, because LineageOS builds it into /system_ext/priv-app/ with the
         platform certificate. GMS is not platform-signed, which is exactly why
         GMS needed the slot and the incumbent never did.

         Proven by control on the flashed build, not inferred: with this line in
         place, `device_config delete privacy location_indicators_enabled` read
         back null, a reboot read back true, and the log carried
         "I SimpleDeviceConfig: Updating device config at boot". All 8 survive.

         ⚠️⚠️ AND THE COST IS REAL ANYWAY, BY A THIRD MECHANISM. The guard written
         for the second answer fired on the flashed build: SimpleDeviceConfig
         writes its 8 at boot, and then GMS OVERWRITES them, because GMS now
         pushes values into those same namespaces. Measured after a checkin,
         against a capture taken before this line existed:

             privacy/location_indicators_enabled          true    to false
             privacy/location_indicator_settings_enabled  true    to unset
             privacy/discrete_history_ops_cslist          42 ops  to unset
             attention_manager_service/keep_screen_on_enabled false to unset
             mediaprovider/allowed_cloud_providers        photos  to unset
             autofill/compat_mode_allowed_packages        21 pkgs to Google's, no Chrome
             backup_and_restore/…future_timeout_millis    900000  to 600000
             backup_and_restore/…callback_timeout_millis  900000  to 300000

         ★ OWNER'S DECISION, session 35: do not pin any of them.
         `device_config override` makes a flag ignore server updates; it was
         offered and declined. Handing GMS the slot means handing it this table,
         and the device behaves like any normal GMS device instead of carrying a
         local special case. **The location privacy indicator is off, knowingly.**

         ★ The lesson, and three wrong answers is what it cost: a protection
         level with several flags does not tell you WHICH flag is doing the work
         for an existing holder, and displacing a holder is not the only way to
         lose its values. Reading AOSP's empty default said "free slot". Reading
         the device's overlay said "occupied, costs 8". `dumpsys package <pkg> |
         grep signatures` said "occupied and free". Only running it and measuring
         afterwards said what was true. **Every one of the first three was a read;
         the answer needed a flash.**

         ✔ work/scripts/55-seed-deviceconfig.py is the standing report for this:
         it parses upstream's own overlay (so the list cannot drift when
         LineageOS edits theirs) and prints upstream's value against the live one.
         It changes nothing by default. Differences are expected.

         Acceptance: `cmd overlay lookup` with the verbose flag, on
         android:string/config_deviceConfiguratorPackageName, shows the "Overlaid:"
         line; `grep -c "Permission denial to mutate flag"` over a full boot is 0;
         and `device_config list` gains keys, diffed against a capture taken before
         the change. -->
    <string name="config_deviceConfiguratorPackageName" translatable="false">com.google.android.gms</string>

    <!-- ================================================================== -->
    <!-- App compatibility: four AOSP PHONE defaults this tablet inherited   -->
    <!-- ================================================================== -->

    <!-- ★ All four below resolve from AOSP's `values/` with NO competing
         qualifier — read out of the BUILT framework-res.apk with
         `aapt2 dump resources`, where the ()/(sw600dp) marker IS the qualifier
         directory. That is the only instrument that does not fall into fact 37,
         and it matters: three neighbouring resources checked the same way turned
         out to come from values-sw600dp/ and be the OPPOSITE of what values/
         says (config_allowAllRotations true, config_navBarCanMove false,
         config_dockedStackDividerSnapMode 1). Those three needed no overlay.
         These four do.

         The neighboring
         config_letterboxIsDisplayAspectRatioForFixedOrientationLetterboxEnabled
         and config_letterboxIsSplitScreenAspectRatioForUnresizableAppsEnabled
         remain deliberately off: they change layout for every affected app and
         are too broad for what they buy. -->

    <!-- In standard split-screen and Activity Embedding, one resumed activity
         can be visible without owning real focus. AOSP's own resource comment
         notes that some game engines wait for focus before drawing, so fake focus
         keeps that visible pane usable without changing its windowing mode. -->
    <bool name="config_isCompatFakeFocusEnabled">true</bool>

    <!-- A translucent activity launched from a letterboxed parent should inherit
         the parent's compatibility bounds. Otherwise a permission dialog, share
         sheet or photo picker can span the display while the activity that raised
         it remains letterboxed. -->
    <bool name="config_letterboxIsEnabledForTranslucentActivities">true</bool>

    <!-- The folio keyboard is a first-class input on this device (its own
         keylayout, seven bound keys, a whole MalbecParts page). AOSP's phone
         default means no menu ever shows a keyboard-shortcut label —
         ViewConfiguration.java:596 gates it and MenuBuilder.java:765-769 reads
         that gate. Free value the moment a keyboard is attached. -->
    <bool name="config_showMenuShortcutsWhenKeyboardPresent">true</bool>

    <!-- ★ Camera compatibility. Provably DEAD before this line, and the whole
         chain was walked rather than assumed:
           * AppCompatCameraDisplayRotationPolicy.isPolicyEnabled() is
             !Flags.cameraCompatUnifyCameraPolicies() && …EnabledAtBuildTime()
             -> true && FALSE
           * AppCompatCameraPolicy's constructor then takes the else branch and
             leaves mDisplayRotationPolicy, mSimReqOrientationPolicy,
             mCameraStateMonitor and mActivityRefresher ALL NULL
           * SynchedDeviceConfig.java:183 returns
             mBuildTimeFlagEnabled && mOverrideValue, so NO server-pushed
             window_manager flag can rescue it. The resource is the only door.

         Why it matters on THIS tablet specifically: universal_resizable_by_default
         decodes as ENABLED in the built aconfig cache, so DisplayContent.java:6774-6781
         makes ignoreOrientationRequest true at sw1095dp — portrait-only apps
         LETTERBOX instead of rotating the display. That makes "portrait-locked
         app opens a camera on a landscape tablet and the preview is sideways"
         a normal large-screen compatibility case rather than an edge case.

         ⚠️ Only the DISPLAY-ROTATION half will activate.
         AppCompatCameraSimReqOrientationPolicy.isPolicyEnabled() additionally
         requires DesktopModeHelper.canEnterDesktopMode(), and this tree sets
         config_isDesktopModeSupported=false deliberately (see the desktop
         windowing block above). That is the right trade and it is not an
         accident — but do not read this line as enabling both policies.

         ⚠️ The SYMPTOM was never measured on this device: no third-party camera
         app is installed here. The MECHANISM is proven off. Acceptance is the
         owner opening a camera inside WeChat / Taobao / Weibo in landscape,
         before and after — not a log line. -->
    <bool name="config_isWindowManagerCameraCompatTreatmentEnabled">true</bool>

</resources>
