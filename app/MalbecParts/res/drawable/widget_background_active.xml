<?xml version="1.0" encoding="utf-8"?>
<!--
     SPDX-FileCopyrightText: The LineageOS Project
     SPDX-License-Identifier: Apache-2.0

     Game-mode card. Same shape as widget_background.xml, accent fill.

     ⚠️ Two drawables rather than one plus a colour setter, on purpose.
     RemoteViews.setColorInt(id, "setBackgroundColor", …) resolves to
     View.setBackgroundColor, which REPLACES the background with a ColorDrawable
     and therefore throws the corner radius away — which is exactly how the first
     version of this widget ended up with square corners. setBackgroundResource is
     @RemotableViewMethod (View.java:26633) and swaps the whole shape, so the
     radius cannot be lost. Two eight-line files is the cheap price for that.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="@android:dimen/system_app_widget_background_radius" />
    <solid android:color="@color/widget_bg_active" />
</shape>
